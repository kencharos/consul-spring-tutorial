spring:
  application:
    name: sprnigcloudgateway
  cloud:
    # omit consul config, use default
    gateway:
      discovery: # use consul service discovery
        locator:
          enabled: true
    loadbalancer:
      ribbon:
        enabled: false
